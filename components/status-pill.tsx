import type { AssetStatus } from '@/lib/types';

const labels: Record<AssetStatus, string> = { assigned: 'Assigned', available: 'Available', in_storage: 'In storage', under_repair: 'Under repair', missing: 'Missing', retired: 'Retired', maintenance: 'Under repair' };
export function StatusPill({ status }: { status: AssetStatus }) { const tone = status === 'assigned' ? 'bg-sage text-moss' : status === 'available' || status === 'in_storage' ? 'bg-[#f4eadb] text-[#9a6827]' : status === 'under_repair' || status === 'maintenance' ? 'bg-[#f0e4e1] text-[#a25c4c]' : status === 'missing' ? 'bg-[#f8e9e5] text-[#a25c4c]' : 'bg-[#e8e9e7] text-ink/55'; return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[11px] font-semibold ${tone}`}><i className="h-1.5 w-1.5 rounded-full bg-current" />{labels[status]}</span>; }