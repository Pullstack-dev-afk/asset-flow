import { ArrowDownLeft, ArrowUpRight, ClipboardList, Search } from 'lucide-react';
import { getHistory } from '@/lib/data';
import { PageHeader } from '@/components/page-header';

export default async function HistoryPage() {
  const history = await getHistory();
  return <div className="mx-auto max-w-[1400px] px-6 pb-12 pt-9 sm:px-10 lg:px-14 lg:pt-12"><PageHeader eyebrow="Audit trail" title="History" description="A complete record of every asset movement." /><div className="mb-5 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search size={16} className="absolute left-3 top-3 text-ink/35" /><input placeholder="Search history" className="h-10 w-full border border-[#dfe5df] bg-white pl-10 pr-4 text-sm outline-none placeholder:text-ink/35 focus:border-moss" /></div><select className="h-10 border border-[#dfe5df] bg-white px-3 text-xs font-medium text-ink/65 outline-none"><option>All activity</option><option>Assignments</option><option>Storage moves</option></select></div><div className="border border-[#dfe5df] bg-white"><div className="flex items-center gap-3 border-b border-[#e6ebe6] bg-[#fbfcf9] px-5 py-3.5 text-[10px] font-semibold uppercase tracking-[0.12em] text-ink/40"><ClipboardList size={14} /> Recent activity</div><div className="divide-y divide-[#edf0ed]">{history.map((event) => <div key={event.id} className="flex items-center gap-4 px-5 py-4"><div className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-full ${event.action === 'Added asset' ? 'bg-sage text-moss' : event.action === 'Moved to storage' ? 'bg-[#f4eadb] text-[#9a6827]' : 'bg-[#e5e9f0] text-[#60718e]'}`}>{event.action === 'Moved to storage' ? <ArrowDownLeft size={16} /> : <ArrowUpRight size={16} />}</div><div className="min-w-0 flex-1"><p className="truncate text-[13px] font-semibold text-ink"><span className="font-normal text-ink/55">{event.action}</span> · {event.asset}</p><p className="mt-1 text-xs text-ink/45">{event.actor} moved this to <span className="text-ink/70">{event.target}</span></p></div><div className="shrink-0 text-right"><p className="text-xs font-medium text-ink/60">{event.date}</p><p className="mt-1 text-[11px] text-ink/35">{event.time}</p></div></div>)}</div></div></div>;
}
